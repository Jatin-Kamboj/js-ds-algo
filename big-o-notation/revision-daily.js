function sumArray(arr) {
  let sum = 0; // a single variable, takes constant space
  for (let i = 0; i < arr.length; i++) {
    sum += arr[i]; // iterating over array, but no extra space required
  }
  return sum;
}

// Linear time compoexity
// This means the time increases with the no of operations to be equally propotional to the number of elements in the array


/**
 * Read the above example for the complexity names and the terms 
 * which we rse gnna use to see how much time an alog takes and how we can use
 * those terms to showcase and express them to others.
 */


// Click a button 
// retrive most recent tweet and their oldest tweet

// Create an API endpoint
// Query to fetch first tweet order by created_at desc  
// Query to fetch last tweet order by created_at asc

// O(1)

// Hello Jatin Kamboj Senior Software Engineer